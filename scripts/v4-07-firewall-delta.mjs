const replaceOnce = (text, from, to) => {
  if (text.split(from).length !== 2) throw new Error(`V4.07 firewall anchor changed: ${from.slice(0, 140)}`);
  return text.replace(from, to);
};

const OLD_DE = "        {key:'external_services',title:'Externe Dienste & Netzwerkfreigaben',paragraphs:['Bei Firewalls, VLANs, DNS-Filtern oder Proxys müssen die folgenden Laufzeitziele vom Gerät mit Browser bzw. Home-Assistant-Companion-App erreichbar sein. Eine Freigabe nur für den Home-Assistant-Server reicht für diese Frontend-Aufrufe nicht zwingend.'],entries:[['Open-Meteo Geocoding','geocoding-api.open-meteo.com · HTTPS/TCP 443 · primäre weltweite Orts-/PLZ-Suche. Übertragen werden Suchbegriff, Sprache und nur bei expliziter Länderauswahl der ISO-Ländercode.'],['OpenStreetMap Nominatim','nominatim.openstreetmap.org · HTTPS/TCP 443 · Rückfall-Geocoding, wenn Open-Meteo kein hinreichendes Ergebnis liefert. Gewitterradar begrenzt öffentliche Nominatim-Aufrufe auf mindestens eine Sekunde Abstand.'],['Leaflet-Laufzeitbibliothek','unpkg.com · HTTPS/TCP 443 · wird aktuell für Leaflet 1.9.4 geladen. Wird dieser Host blockiert, kann die Karte nicht initialisiert werden.'],['OpenStreetMap-Kacheln','*.tile.openstreetmap.org · HTTPS/TCP 443 · Kartenkacheln der Leaflet-Darstellung. Bei Sperre können Gewitterradar-Daten vorhanden sein, die Grundkarte bleibt jedoch leer.'],['Blitzortung','Die separate Home-Assistant-Blitzortung-Integration arbeitet serverseitig vom Home-Assistant-System aus. Deren Netzwerkfreigaben sind deshalb getrennt von den oben genannten clientseitigen Gewitterradar-Zielen zu betrachten.']],notes:['Die lokale Länder-Autovervollständigung erzeugt keine externen Geocoding-Anfragen.','Sind Open-Meteo und Nominatim beide blockiert, schlägt die weltweite Ortssuche fehl; bereits gespeicherte Orte bleiben lokal nutzbar.','Nur tatsächlich verwendete Laufzeitziele sind hier aufgeführt; GitHub/HACS gehören zur Installation bzw. Aktualisierung und nicht zur normalen Kartenlaufzeit.']},\n";

const NEW_DE = "        {key:'external_services',title:'Externe Dienste & Netzwerkfreigaben',paragraphs:['Für Firewalls, VLANs, segmentierte Netze, DNS-Filter und Proxys ist entscheidend, von welchem Gerät die Verbindung ausgeht. Gewitterradar trennt clientseitige Karten-/Suchaufrufe vom serverseitigen Blitzortung-Datenstrom.','Clientseitig bedeutet: Das Endgerät mit Browser oder Home-Assistant-Companion-App benötigt die Freigabe. Serverseitig bedeutet: Der Home-Assistant-Server benötigt die Freigabe. Eine Freigabe nur an einer der beiden Stellen reicht deshalb nicht für alle Funktionen.'],entries:[['Ortssuche – primär','CLIENT → geocoding-api.open-meteo.com · HTTPS/TCP 443 · erforderlich für die primäre weltweite Orts-/PLZ-Suche. Übertragen werden Suchbegriff, Sprache und nur bei ausdrücklich gewähltem Land der ISO-Ländercode. Wird das Ziel blockiert, versucht Gewitterradar den Rückfalldienst.'],['Ortssuche – Rückfall','CLIENT → nominatim.openstreetmap.org · HTTPS/TCP 443 · Rückfall-Geocoding, wenn Open-Meteo kein hinreichendes Ergebnis liefert. Öffentliche Aufrufe werden auf mindestens eine Sekunde Abstand begrenzt. Sind beide Geocoding-Ziele gesperrt, funktioniert die neue Ortssuche nicht; bereits gespeicherte Orte bleiben lokal nutzbar.'],['Kartenbibliothek','CLIENT → unpkg.com · HTTPS/TCP 443 · lädt Leaflet 1.9.4 JavaScript und CSS. Diese Freigabe ist für die aktuelle Leaflet-Karteninitialisierung erforderlich. Bei Sperre kann die Karte nicht initialisiert werden.'],['OpenStreetMap-Grundkarte','CLIENT → *.tile.openstreetmap.org · HTTPS/TCP 443 · liefert die Kartenkacheln. Bei Sperre können Home-Assistant-/Blitzdaten weiterhin vorhanden sein, die geografische Grundkarte bleibt jedoch leer.'],['Blitzdaten','SERVER → blitzortung.ha.sed.pl · MQTT/TCP 1883 · erforderlich für die separate Blitzortung-Home-Assistant-Integration. Die aktuelle Upstream-Integration nutzt MQTT 3.1.1 auf TCP 1883 ohne TLS. Diese Verbindung muss ausgehend vom Home-Assistant-System erlaubt und per DNS auflösbar sein.'],['Blitzortung-Kartenlink','OPTIONALER CLIENT-AUFRUF → map.blitzortung.org · HTTPS/TCP 443 · nur relevant, wenn ein von Blitzortung angebotener externer Kartenlink geöffnet wird; für die normale Gewitterradar-Darstellung nicht erforderlich.']],notes:['Empfohlene Firewall-Richtung: ausschließlich ausgehende Verbindungen zu den dokumentierten Zielen freigeben; eingehende Internet-Freigaben werden von Gewitterradar dafür nicht benötigt.','Die lokale Länder-Autovervollständigung und die Gewitterradar-To-do-Ortsbibliothek arbeiten ohne externe Netzwerkaufrufe.','MQTT/TCP 1883 ist in der verwendeten Blitzortung-Integration unverschlüsselt. Wenn eine Sicherheitsrichtlinie unverschlüsseltes ausgehendes MQTT verbietet, ist das eine Einschränkung der Blitzortung-Integration und sollte nicht durch pauschale oder weitreichende Firewall-Ausnahmen umgangen werden.','GitHub/HACS sind Installations-/Aktualisierungswege und keine Voraussetzung für die normale Kartenlaufzeit; deren möglichen Netzwerkziele werden deshalb bewusst nicht als Gewitterradar-Laufzeitfreigaben aufgeführt.','Fehlerbild bei Firewall-Sperren: keine Karte → zuerst unpkg.com prüfen; leere Grundkarte → OSM-Kacheln prüfen; Ortssuche ohne Treffer/Fehler → Open-Meteo und Nominatim prüfen; keine neuen Blitze/Sensoren nicht verfügbar → serverseitig DNS sowie blitzortung.ha.sed.pl:1883 prüfen.']},\n";

const OLD_EN = "        {key:'external_services',title:'External services & network access',paragraphs:['With firewalls, VLANs, DNS filtering or proxies, the following runtime destinations must be reachable from the device running the browser or Home Assistant Companion App. Allowing only the Home Assistant server is not necessarily sufficient for these frontend requests.'],entries:[['Open-Meteo Geocoding','geocoding-api.open-meteo.com · HTTPS/TCP 443 · primary worldwide place/postcode search. The query, language and only when explicitly selected the ISO country code are transmitted.'],['OpenStreetMap Nominatim','nominatim.openstreetmap.org · HTTPS/TCP 443 · fallback geocoding when Open-Meteo has no sufficiently suitable result. Gewitterradar limits public Nominatim requests to at least one second apart.'],['Leaflet runtime library','unpkg.com · HTTPS/TCP 443 · currently loads Leaflet 1.9.4. If this host is blocked, the map cannot initialize.'],['OpenStreetMap tiles','*.tile.openstreetmap.org · HTTPS/TCP 443 · base-map tiles for the Leaflet view. If blocked, Gewitterradar data may still exist but the base map remains empty.'],['Blitzortung','The separate Home Assistant Blitzortung integration runs server-side from the Home Assistant system. Its network rules must therefore be handled separately from the client-side Gewitterradar destinations above.']],notes:['Local country autocomplete performs no external geocoding requests.','If both Open-Meteo and Nominatim are blocked, worldwide place search fails; already saved places remain available locally.','Only actual runtime destinations are listed here; GitHub/HACS are installation/update dependencies, not normal map-runtime dependencies.']},\n";

const NEW_EN = "        {key:'external_services',title:'External services & network access',paragraphs:['For firewalls, VLANs, segmented networks, DNS filtering and proxies, the source device matters. Gewitterradar separates client-side map/search traffic from the server-side Blitzortung lightning-data stream.','CLIENT means the browser or Home Assistant Companion App device needs access. SERVER means the Home Assistant host needs access. Allowing only one side is therefore insufficient for all functions.'],entries:[['Place search – primary','CLIENT → geocoding-api.open-meteo.com · HTTPS/TCP 443 · required for the primary worldwide place/postcode search. The query, language and only an explicitly selected ISO country code are transmitted. If blocked, Gewitterradar attempts the fallback service.'],['Place search – fallback','CLIENT → nominatim.openstreetmap.org · HTTPS/TCP 443 · fallback geocoding when Open-Meteo has no sufficiently suitable result. Public calls are limited to at least one second apart. If both geocoding destinations are blocked, worldwide search fails; saved places remain locally usable.'],['Map library','CLIENT → unpkg.com · HTTPS/TCP 443 · loads Leaflet 1.9.4 JavaScript and CSS. This is required for the current Leaflet map initialization. If blocked, the map cannot initialize.'],['OpenStreetMap base map','CLIENT → *.tile.openstreetmap.org · HTTPS/TCP 443 · provides map tiles. If blocked, Home Assistant/lightning data may still exist but the geographic base map stays empty.'],['Lightning data','SERVER → blitzortung.ha.sed.pl · MQTT/TCP 1883 · required by the separate Blitzortung Home Assistant integration. The current upstream integration uses MQTT 3.1.1 on TCP 1883 without TLS. Outbound access and DNS resolution must therefore be available from the Home Assistant host.'],['Blitzortung map link','OPTIONAL CLIENT REQUEST → map.blitzortung.org · HTTPS/TCP 443 · only relevant when opening an external map link offered by Blitzortung; not required for normal Gewitterradar rendering.']],notes:['Recommended firewall direction: allow only outbound connections to the documented destinations; Gewitterradar does not require inbound Internet rules for these functions.','Local country autocomplete and the Gewitterradar local to-do place library make no external network requests.','MQTT/TCP 1883 is unencrypted in the currently used Blitzortung integration. If policy forbids unencrypted outbound MQTT, treat this as an upstream integration constraint rather than bypassing policy with broad firewall exceptions.','GitHub/HACS are installation/update paths, not normal map-runtime dependencies, and are intentionally excluded from the Gewitterradar runtime allowlist.','Firewall symptom guide: no map → check unpkg.com; empty base map → check OSM tiles; place-search failure → check Open-Meteo/Nominatim; no new lightning or unavailable sensors → check server-side DNS and blitzortung.ha.sed.pl:1883.']},\n";

export function v407FirewallDelta(source) {
  let text = source;
  text = replaceOnce(text,"const GEWITTERRADAR_BUILD = 'V4.07-TEST3-2026-09-12';","const GEWITTERRADAR_BUILD = 'V4.07-TEST4-2026-09-12';");
  text = replaceOnce(text,OLD_DE,NEW_DE);
  text = replaceOnce(text,OLD_EN,NEW_EN);
  return text;
}
