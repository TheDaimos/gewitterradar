const replaceOnce = (text, from, to) => {
  if (text.split(from).length !== 2) throw new Error(`V4.07 network-security anchor changed: ${from.slice(0, 140)}`);
  return text.replace(from, to);
};

const replaceRangeOnce = (text, startMarker, endMarker, replacement) => {
  const start = text.indexOf(startMarker);
  if (start < 0) throw new Error(`V4.07 network-security range start missing: ${startMarker}`);
  const end = text.indexOf(endMarker, start + startMarker.length);
  if (end < 0) throw new Error(`V4.07 network-security range end missing: ${endMarker}`);
  const secondStart = text.indexOf(startMarker, start + startMarker.length);
  if (secondStart >= 0) throw new Error(`V4.07 network-security range start is not unique: ${startMarker}`);
  return text.slice(0, start) + replacement + text.slice(end);
};

const EXTERNAL_SERVICES_DE = `        {key:'external_services',title:'Externe Dienste & Netzwerkfreigaben',paragraphs:['Gewitterradar verwendet zwei getrennte Netzwerkpfade: Karten- und Ortssuchanfragen kommen direkt vom Endgerät mit Browser bzw. Home-Assistant-Companion-App; die Blitzortung-Live-Daten werden dagegen von der separaten Blitzortung-Integration auf dem Home-Assistant-Server bezogen. Bei Firewall, VLAN-/Netzsegmentierung, Proxy, DNS-Filter oder TLS-Inspection müssen deshalb beide Pfade getrennt freigegeben und geprüft werden.','Für eine strikte ausgehende Freigabeliste sind die nachfolgenden Ziele der aktuelle V4.07-Laufzeitbestand. Die lokale Ortsbibliothek, der Gewitterradar-Tracker und die Home-Assistant-Services arbeiten lokal und benötigen dafür keinen Internetzugriff.'],entries:[['Client · Open-Meteo Geocoding','geocoding-api.open-meteo.com · HTTPS/TCP 443 · primäre weltweite Orts-/PLZ-Suche. Übertragen werden Suchbegriff und Sprache sowie nur bei einer ausdrücklich gewählten Ländereinschränkung der ISO-Ländercode. Ist der Dienst nicht erreichbar, versucht Gewitterradar den Nominatim-Rückfall.'],['Client · OpenStreetMap Nominatim','nominatim.openstreetmap.org · HTTPS/TCP 443 · optionaler Rückfall für die Ortssuche. Übertragen werden Suchbegriff und Sprache sowie bei expliziter Länderauswahl der ISO-Ländercode. Öffentliche Nominatim-Aufrufe werden auf mindestens eine Sekunde Abstand begrenzt; beim Tippen in die lokale Länder-Autovervollständigung erfolgen keine Nominatim-Anfragen. Sind Open-Meteo und Nominatim beide blockiert, ist die Online-Ortssuche nicht verfügbar; gespeicherte Orte bleiben lokal nutzbar.'],['Client · Leaflet','unpkg.com · HTTPS/TCP 443 · lädt im aktuellen Kartenpfad Leaflet 1.9.4 als JavaScript und CSS. Bei einem Kaltstart ohne bereits vorhandene Leaflet-Laufzeit kann die Karte bei Sperre dieses Hosts nicht initialisiert werden.'],['Client · OpenStreetMap-Kacheln','a.tile.openstreetmap.org, b.tile.openstreetmap.org und c.tile.openstreetmap.org · HTTPS/TCP 443 · Grundkartenkacheln. Bei Sperre kann Leaflet weiterhin laufen und Gewitterradar-Daten können vorhanden sein, die Grundkarte bleibt jedoch leer bzw. unvollständig.'],['Server · Blitzortung v1.7.1','blitzortung.ha.sed.pl · MQTT/TCP 1883 · ausgehend vom Home-Assistant-Server. Die separate Blitzortung-Integration v1.7.1 verwendet diesen Broker für Live-Blitzdaten und abonniert die zur gewählten Region passenden Geohash-Themen. Der aktuelle Upstream-Pfad nutzt dabei MQTT auf TCP 1883 und nicht HTTPS. Bei Sperre kann Blitzortung nicht verbinden bzw. keine neuen Live-Blitze liefern, während Ortssuche und Kartenabrufe davon unabhängig bleiben.'],['Lokal · keine externe Freigabe','device_tracker.gewitterradar bzw. device_tracker.gewitterradar_dashboard, gewitterradar.set_reference_coordinates, das Dashboard-Setz-Script und die Local-To-do-Liste „Gewitterradar Orte“ laufen innerhalb Home Assistant. Für diese Funktionen ist keine zusätzliche Internet-Domain erforderlich.'],['Installation / Aktualisierung','GitHub/HACS sind für Installation und Aktualisierung relevant, aber keine Gewitterradar-Laufzeitabhängigkeit der geöffneten Karte. Die von HACS verwendeten GitHub-Auslieferungsziele können sich unterscheiden und sollten über die vorhandene HACS-/Update-Freigaberichtlinie behandelt werden, nicht als dauerhafte Gewitterradar-Laufzeitfreigabe.'],['Schnelldiagnose','Ortssuche gestört, Karte funktioniert: Open-Meteo/Nominatim prüfen. Karte startet nicht: unpkg.com prüfen. Kartenrahmen vorhanden, aber Grundkarte leer: a/b/c.tile.openstreetmap.org prüfen. Suche und Karte funktionieren, aber Live-Blitze fehlen oder bleiben alt: vom Home-Assistant-Server blitzortung.ha.sed.pl:1883 und den Zustand der Blitzortung-Integration prüfen.']],notes:['DNS-Auflösung muss sowohl auf dem Endgerät als auch auf dem Home-Assistant-Server zu den jeweils benötigten Zielen funktionieren.','HTTPS-Proxy, TLS-Inspection, Inhaltsfilter, Werbe-/Tracking-Blocker oder Browser-Erweiterungen können Frontend-Anfragen trotz offener TCP-443-Regel blockieren; bei Geocoding zusätzlich CORS/Antwortfilterung berücksichtigen.','Datenschutz: Geocoding-Dienste erhalten den Suchbegriff und die genannten Parameter. OpenStreetMap-Kachelserver erhalten durch die angeforderten Kacheln technisch Informationen über den betrachteten Kartenausschnitt sowie übliche Verbindungsmetadaten. Der Blitzortung-Broker erhält die von der Integration abonnierten Regions-/Geohash-Themen.','Die im JavaScript vorkommende Zeichenfolge http://www.w3.org/2000/svg ist lediglich der SVG-Namensraum und kein externer Netzwerkaufruf bzw. keine Firewall-Freigabe.','Diese Liste ist als V4.07-Laufzeitinventar automatisiert abgesichert: neue feste externe URL-Ziele sollen den Test fehlschlagen lassen, bis Dokumentation und Freigabeliste bewusst nachgezogen wurden.']},\n`;

const EXTERNAL_SERVICES_EN = `        {key:'external_services',title:'External services & network access',paragraphs:['Gewitterradar uses two separate network paths: map and place-search requests originate directly from the device running the browser or Home Assistant Companion App, while Blitzortung live data is fetched by the separate Blitzortung integration on the Home Assistant server. Firewalls, VLAN/network segmentation, proxies, DNS filters or TLS inspection therefore have to allow and diagnose both paths separately.','For strict outbound allowlists, the destinations below are the current V4.07 runtime inventory. The local place library, Gewitterradar tracker and Home Assistant services operate locally and do not require Internet access for those functions.'],entries:[['Client · Open-Meteo Geocoding','geocoding-api.open-meteo.com · HTTPS/TCP 443 · primary worldwide place/postcode search. The search term and language are transmitted, plus the ISO country code only when the user explicitly selects a country restriction. If unavailable, Gewitterradar attempts the Nominatim fallback.'],['Client · OpenStreetMap Nominatim','nominatim.openstreetmap.org · HTTPS/TCP 443 · optional fallback for place search. The search term and language are transmitted, plus the ISO country code for an explicit country selection. Public Nominatim calls are limited to at least one second apart; typing in the local country autocomplete performs no Nominatim request. If both Open-Meteo and Nominatim are blocked, online place search is unavailable while saved places remain usable locally.'],['Client · Leaflet','unpkg.com · HTTPS/TCP 443 · loads Leaflet 1.9.4 JavaScript and CSS for the current map path. On a cold start without an already available Leaflet runtime, blocking this host prevents the map from initializing.'],['Client · OpenStreetMap tiles','a.tile.openstreetmap.org, b.tile.openstreetmap.org and c.tile.openstreetmap.org · HTTPS/TCP 443 · base-map tiles. If blocked, Leaflet may still run and Gewitterradar data may exist, but the base map remains blank or incomplete.'],['Server · Blitzortung v1.7.1','blitzortung.ha.sed.pl · MQTT/TCP 1883 · outbound from the Home Assistant server. The separate Blitzortung integration v1.7.1 uses this broker for live lightning data and subscribes to geohash topics matching the selected region. The current upstream path uses MQTT on TCP 1883 rather than HTTPS. If blocked, Blitzortung cannot connect or deliver new live strikes, while place search and map requests remain independent.'],['Local · no external rule required','device_tracker.gewitterradar or device_tracker.gewitterradar_dashboard, gewitterradar.set_reference_coordinates, the Dashboard setter script and the Local to-do list “Gewitterradar Orte” operate inside Home Assistant. No additional Internet domain is required for these functions.'],['Installation / updates','GitHub/HACS are relevant to installation and updates but are not a Gewitterradar runtime dependency of an open map. GitHub delivery hosts used by HACS can vary and should be handled by the existing HACS/update allowlist policy rather than as a permanent Gewitterradar runtime rule.'],['Quick diagnosis','Place search fails while the map works: check Open-Meteo/Nominatim. Map does not start: check unpkg.com. Map frame is present but the base map is blank: check a/b/c.tile.openstreetmap.org. Search and map work but live strikes are missing or stale: from the Home Assistant server check blitzortung.ha.sed.pl:1883 and the Blitzortung integration state.']],notes:['DNS resolution must work on both the client device and the Home Assistant server for the destinations each side needs.','HTTPS proxies, TLS inspection, content filters, ad/tracking blockers or browser extensions can block frontend requests even when TCP 443 is open; for geocoding also consider CORS/response filtering.','Privacy: geocoding providers receive the search term and the parameters listed above. OpenStreetMap tile servers technically learn the requested map tiles/viewport plus normal connection metadata. The Blitzortung broker receives the region/geohash topics subscribed by the integration.','The JavaScript string http://www.w3.org/2000/svg is only the SVG namespace and is not an external network request or firewall requirement.','This list is guarded as the V4.07 runtime inventory: new fixed external URL destinations should fail the test until documentation and the allowlist are deliberately updated.']},\n`;

export function v407NetworkSecurityDelta(source) {
  let text = source;
  text = replaceOnce(text,
    "const GEWITTERRADAR_BUILD = 'V4.07-TEST3-2026-09-12';",
    "const GEWITTERRADAR_BUILD = 'V4.07-TEST4-2026-09-12';");

  text = replaceRangeOnce(text,
    "        {key:'external_services',title:'Externe Dienste & Netzwerkfreigaben'",
    "        {key:'functions',title:'Wichtige Funktionen'",
    EXTERNAL_SERVICES_DE);
  text = replaceRangeOnce(text,
    "        {key:'external_services',title:'External services & network access'",
    "        {key:'functions',title:'Key functions'",
    EXTERNAL_SERVICES_EN);
  return text;
}
