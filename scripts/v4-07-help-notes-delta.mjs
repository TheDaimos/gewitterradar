const replaceOnce = (text, from, to) => {
  if (text.split(from).length !== 2) throw new Error(`V4.07 help anchor changed: ${from.slice(0, 120)}`);
  return text.replace(from, to);
};

const LOCATION_HELP_DE = {
  from: "        {key:'location',title:'Referenzstandort',paragraphs:['Der Referenzstandort bestimmt, von welchem Punkt aus Entfernungen und Richtungen berechnet werden. Gewitterradar erkennt person.*- und zone.*-Entitäten dynamisch. Ändert sich der Standort einer Person, werden Entfernungen auf Basis der aktuellen Home-Assistant-Daten neu bewertet.'],notes:['Ein falscher Referenzstandort führt zu falschen Entfernungen, Radien und Kompassrichtungen.']},",
  to: "        {key:'location',title:'Referenzstandort',paragraphs:['Der Referenzstandort bestimmt, von welchem Punkt aus Entfernungen und Richtungen berechnet werden. Für die weltweite Ortssuche verwendet die Dashboard-/Package-Variante von V4.07 device_tracker.gewitterradar_dashboard. Damit auch die Blitzdaten dem gewählten Ort folgen, muss Blitzortung selbst diesen Tracker als Standortquelle verfolgen.','Einrichtung in Blitzortung: Einstellungen → Geräte & Dienste → Blitzortung → Eintrag hinzufügen. Als Konfigurationstyp „Location entity“ wählen, danach als „Location entity“ den Eintrag „Gewitterradar Dashboard“ auswählen. Wurde ein bestehender Blitzortung-Eintrag mit festen Breiten-/Längengraden angelegt, lässt er sich über „Neu konfigurieren“ nicht auf eine Standort-Entität umstellen. In diesem Fall einen neuen Blitzortung-Eintrag anlegen, den neuen Eintrag prüfen und den alten Koordinaten-Eintrag erst danach deaktivieren oder löschen.','Danach in den Blitzortung-Optionen Erfassungsradius, Zeitfenster und maximale Blitzanzahl passend wählen. Für die V4.07-Erprobung sind 500 km Erfassungsradius, 120 Minuten Zeitfenster und 200 Blitze sinnvolle Beispielwerte. Der Erfassungsradius muss mindestens den benötigten Gewitterradar-Beobachtungsbereich abdecken, sollte aber nicht unnötig groß sein, weil die Mindeststrecke für eine Standortaktualisierung durch Blitzortung vom Erfassungsradius abhängt.','„Nutzen“ übernimmt den gewählten Ort sofort als Bezugsstandort, schließt die Ortssuche automatisch und fährt die Karte direkt zum neuen Standort. Für „★ Speichern“ einmalig unter Einstellungen → Geräte & Dienste → Integration hinzufügen eine lokale To-do-Liste mit dem Namen „Gewitterradar Orte“ anlegen. Gespeicherte Treffer erscheinen danach im Standortmenü unter „Gespeicherte Orte“ und können von dort wieder direkt verwendet werden.'],notes:['Ein Kartenwechsel allein beweist nicht, dass Blitzortung seine Live-Datenregion bereits umgestellt hat. Bei der nativen Integration wird statt des Dashboard-Trackers device_tracker.gewitterradar verwendet. Die Werte 500 km / 120 min / 200 sind Beispielwerte für den Test und keine zwingenden Vorgaben. Die Ortsbibliothek liegt lokal in Home Assistant; Gewitterradar speichert darin Name und Koordinaten des gewählten Ortes.']},"
};

const LOCATION_HELP_EN = {
  from: "        {key:'location',title:'Reference location',paragraphs:[\"The reference location determines the point from which distances and directions are calculated. Gewitterradar detects person.* and zone.* entities dynamically. When a person's location changes, distances are recalculated from the current Home Assistant data.\"],notes:['An incorrect reference location leads to incorrect distances, radius evaluation and compass directions.']},",
  to: "        {key:'location',title:'Reference location',paragraphs:[\"The reference location determines the point from which distances and directions are calculated. In V4.07 the Dashboard/package delivery uses device_tracker.gewitterradar_dashboard for worldwide place search. For lightning data to follow the selected place, Blitzortung itself must track this entity as its location source.\",\"Blitzortung setup: Settings → Devices & services → Blitzortung → Add entry. Choose 'Location entity' as the configuration type, then select 'Gewitterradar Dashboard' as the location entity. If an existing Blitzortung entry was created with fixed latitude/longitude values, Reconfigure cannot convert that entry to an entity-tracked location. Create a new Blitzortung entry, verify it first, and only then disable or remove the old fixed-coordinate entry.\",\"Then choose suitable Blitzortung options for detection radius, time window and maximum number of lightnings. For V4.07 testing, 500 km detection radius, 120 minutes and 200 lightnings are useful example values. The detection radius must cover the required Gewitterradar observation area, but should not be unnecessarily large because Blitzortung's minimum movement before updating a tracked location depends on that radius.\",\"Use applies the selected place immediately, closes the place-search dialog and moves the map directly to the new reference location. To enable ★ Save, create a Local to-do list named 'Gewitterradar Orte' once under Settings → Devices & services → Add integration. Saved results then appear under Saved places and can be used again directly.\"],notes:['A map move alone does not prove that Blitzortung has already moved its live-data region. The native integration uses device_tracker.gewitterradar instead of the Dashboard tracker. The values 500 km / 120 min / 200 are test examples, not mandatory settings. The place library stays local in Home Assistant and stores the selected place name and coordinates.']},"
};

const EXTERNAL_SERVICES_DE = "        {key:'external_services',title:'Externe Dienste & Netzwerkfreigaben',paragraphs:['Bei Firewalls, VLANs, DNS-Filtern oder Proxys müssen die folgenden Laufzeitziele vom Gerät mit Browser bzw. Home-Assistant-Companion-App erreichbar sein. Eine Freigabe nur für den Home-Assistant-Server reicht für diese Frontend-Aufrufe nicht zwingend.'],entries:[['Open-Meteo Geocoding','geocoding-api.open-meteo.com · HTTPS/TCP 443 · primäre weltweite Orts-/PLZ-Suche. Übertragen werden Suchbegriff, Sprache und nur bei expliziter Länderauswahl der ISO-Ländercode.'],['OpenStreetMap Nominatim','nominatim.openstreetmap.org · HTTPS/TCP 443 · Rückfall-Geocoding, wenn Open-Meteo kein hinreichendes Ergebnis liefert. Gewitterradar begrenzt öffentliche Nominatim-Aufrufe auf mindestens eine Sekunde Abstand.'],['Leaflet-Laufzeitbibliothek','unpkg.com · HTTPS/TCP 443 · wird aktuell für Leaflet 1.9.4 geladen. Wird dieser Host blockiert, kann die Karte nicht initialisiert werden.'],['OpenStreetMap-Kacheln','*.tile.openstreetmap.org · HTTPS/TCP 443 · Kartenkacheln der Leaflet-Darstellung. Bei Sperre können Gewitterradar-Daten vorhanden sein, die Grundkarte bleibt jedoch leer.'],['Blitzortung','Die separate Home-Assistant-Blitzortung-Integration arbeitet serverseitig vom Home-Assistant-System aus. Deren Netzwerkfreigaben sind deshalb getrennt von den oben genannten clientseitigen Gewitterradar-Zielen zu betrachten.']],notes:['Die lokale Länder-Autovervollständigung erzeugt keine externen Geocoding-Anfragen.','Sind Open-Meteo und Nominatim beide blockiert, schlägt die weltweite Ortssuche fehl; bereits gespeicherte Orte bleiben lokal nutzbar.','Nur tatsächlich verwendete Laufzeitziele sind hier aufgeführt; GitHub/HACS gehören zur Installation bzw. Aktualisierung und nicht zur normalen Kartenlaufzeit.']},\n";
const EXTERNAL_SERVICES_EN = "        {key:'external_services',title:'External services & network access',paragraphs:['With firewalls, VLANs, DNS filtering or proxies, the following runtime destinations must be reachable from the device running the browser or Home Assistant Companion App. Allowing only the Home Assistant server is not necessarily sufficient for these frontend requests.'],entries:[['Open-Meteo Geocoding','geocoding-api.open-meteo.com · HTTPS/TCP 443 · primary worldwide place/postcode search. The query, language and only when explicitly selected the ISO country code are transmitted.'],['OpenStreetMap Nominatim','nominatim.openstreetmap.org · HTTPS/TCP 443 · fallback geocoding when Open-Meteo has no sufficiently suitable result. Gewitterradar limits public Nominatim requests to at least one second apart.'],['Leaflet runtime library','unpkg.com · HTTPS/TCP 443 · currently loads Leaflet 1.9.4. If this host is blocked, the map cannot initialize.'],['OpenStreetMap tiles','*.tile.openstreetmap.org · HTTPS/TCP 443 · base-map tiles for the Leaflet view. If blocked, Gewitterradar data may still exist but the base map remains empty.'],['Blitzortung','The separate Home Assistant Blitzortung integration runs server-side from the Home Assistant system. Its network rules must therefore be handled separately from the client-side Gewitterradar destinations above.']],notes:['Local country autocomplete performs no external geocoding requests.','If both Open-Meteo and Nominatim are blocked, worldwide place search fails; already saved places remain available locally.','Only actual runtime destinations are listed here; GitHub/HACS are installation/update dependencies, not normal map-runtime dependencies.']},\n";

export function v407HelpNotesDelta(source) {
  let text = source;
  text = replaceOnce(text, LOCATION_HELP_DE.from, LOCATION_HELP_DE.to);
  text = replaceOnce(text, LOCATION_HELP_EN.from, LOCATION_HELP_EN.to);
  text = replaceOnce(text,"        {key:'functions',title:'Wichtige Funktionen'",EXTERNAL_SERVICES_DE + "        {key:'functions',title:'Wichtige Funktionen'");
  text = replaceOnce(text,"        {key:'functions',title:'Key functions'",EXTERNAL_SERVICES_EN + "        {key:'functions',title:'Key functions'");
  return text;
}
