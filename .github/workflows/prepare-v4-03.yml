name: Prepare V4.03 generated files

on:
  push:
    branches:
      - release/v4.03-hacs-no-release-assets
  workflow_dispatch:

permissions:
  contents: write

jobs:
  prepare:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Generate V4.03 sources, HACS distribution and checksums
        run: node scripts/prepare-v4-03.mjs
      - name: Commit generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add gewitterradar-card-v4_03.js gewitterradar-card-v4_03.txt dist/gewitterradar.js SHA256SUMS.txt
          if git diff --cached --quiet; then
            echo "Generated V4.03 files are already current."
            exit 0
          fi
          git commit -m "Generate Gewitterradar V4.03 distribution"
          git push
